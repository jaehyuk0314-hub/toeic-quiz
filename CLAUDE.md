# 토익 단어 OMR

빌드 도구 없이 `index.html` 한 파일로 된 토익 단어 퀴즈. 브라우저에서 파일을 바로 열어 실행한다.

## 규칙

- **답변은 한국어로** 한다.
- **디자인은 지금의 OMR 답안지 스타일을 유지한다.**
  - 색: `:root`의 CSS 변수(`--omr` 빨강 계열, `--good` 초록 등)를 쓴다. 새 색을 하드코딩하지 않는다.
  - 폰트: 제목 Do Hyeon, 본문 IBM Plex Sans KR, 단어·숫자 IBM Plex Mono.
  - 모양: 선택지 앞 타원형 버블(A–D), 상단 빨간 띠가 있는 답안지 카드.
  - 다크 모드 변수 블록 두 곳(`prefers-color-scheme`, `[data-theme="dark"]`)은 항상 같이 고친다.
- **퀴즈는 한 판 20문제, 모두 4지선다 객관식이다.** 다른 형식(주관식, 뜻→단어 등)은 추가하지 않는다.
  - 1~15번: 영어 단어를 보고 한국어 뜻 고르기. 오답 보기는 같은 품사 우선.
  - 16~20번: `ex` 예문에서 단어를 `____`로 바꾼 빈칸 문제, 영어 단어 4개 중 고르기. 단어는 대소문자 무시, 단어 경계 기준으로 찾는다(`employee` 안의 `employ`는 바꾸지 않음). 답을 고르면 완성된 예문과 `exKo`를 보여준다.
  - 빈칸 오답 보기는 `words.js` 전체에서 같은 품사 우선으로 뽑고, 3개 중 최소 2개는 다른 일차 단어로 한다(다른 일차가 없으면 전체에서).
  - 문제가 20개보다 적을 때(오답 복습 등)는 빈칸 문제도 같은 비율(1/4, 내림)로 줄인다.
  - 오답노트·정답률은 두 유형 모두 같은 방식으로 기록한다.
- **발음 듣기**는 브라우저 내장 `speechSynthesis`로 미국식(en-US) 영어를 읽는다. 빈칸 문제에서는 답을 고르기 전에 정답 단어를 읽거나, 화면·스피커 버튼(`data-say`)에 정답 단어를 넣지 않는다.
- **단어는 일차별로 구성한다.** 일차는 기본 50단어이고 보충 단어가 붙을 수 있다(Day 1은 62단어). 일차는 개수로 자르지 않고 각 단어의 `day` 값으로 나눈다. 새 일차는 `words.js` 배열 맨 아래에 이어 붙인다.

## 구조 메모

- 단어 데이터: `words.js`의 `WORDS` 배열. `index.html`이 `<script src="words.js">`로 불러온다. 각 항목은 `{ day, w, pos, ko, ex, exKo }` (일차, 단어, 품사, 뜻, 영어 예문, 예문 해석). 오답노트가 단어(`w`)를 키로 쓰므로 단어는 중복되지 않게 한다.
- 화면 이동: 왼쪽 목차 + 주소 해시(`#today`, `#book-N`, `#quiz`, `#wrong`, `#settings`). 모르는 해시는 `#today`로 바꾼다. 900px 미만에서는 목차가 ☰ 드로어가 된다.
- 기록 저장: `localStorage`의 `toeic-omr-v1`(학습 기록), `toeic-omr-v1-ui`(`view` 마지막 화면, `day` 퀴즈 범위(0=전체), `hide` 뜻 가리기, `autoSpeak` 자동 발음). 저장 형식을 바꾸면 기존 기록이 깨지지 않게 한다.
- 폰에서도 보기 좋아야 한다(좌우 여백 16px, 좁은 화면에서 줄바꿈).
