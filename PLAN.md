# 토익 단어 OMR 개선 계획 (Claude Code용)

이 문서는 작업 지시서다. 사용자가 "PLAN.md N단계 진행해줘"라고 하면 **그 단계만** 구현한다.
다른 단계는 건드리지 않는다. 끝나면 바꾼 내용과 사용자가 직접 확인할 항목을 짧게 알려준다.

## 공통 원칙

- 사용 상황: 매일 아침 폰으로 그날 단어(약 50개)를 외우고 20문제 퀴즈를 푼다. 폰 한 손 조작이 기준.
- 파일 구조 유지: `index.html`(화면·로직), `words.js`(단어 데이터 `WORDS` 배열, `{ day, w, pos, ko, ex, exKo }`). 빌드 도구·프레임워크 추가 금지. GitHub Pages로 그대로 배포된다.
- 디자인 유지: 지금의 OMR 답안지 스타일(분홍 OMR 색, 동그라미 보기, 글꼴, 라이트/다크 토큰)을 그대로 쓴다. 새 색은 기존 CSS 변수에서 파생한다.
- 기존 기록 보존: localStorage에 이미 있는 기록(푼 문제, 정답률, 오답노트, 설정)을 지우거나 키 이름을 바꾸지 않는다. 구조를 바꿔야 하면 기존 값을 읽어 옮기는 코드를 넣는다. localStorage 접근은 try/catch로 감싼다.
- 접근성: 버튼은 `<button>`, 아이콘 버튼에는 aria-label, 키보드 포커스가 보여야 한다.
- 브라우저 `alert/confirm/prompt`는 쓰지 않는다. 확인이 필요하면 화면 안 버튼으로 한다.
- 한 단계가 끝날 때 커밋 메시지 한 줄을 제안한다.

---

## 0단계. 발음 기능 마무리

이미 작업 중인 발음(speechSynthesis) 기능을 점검만 한다.
- 단어장·퀴즈의 스피커 버튼이 동작하는지, 빈칸 문제에서 답을 고르기 전에는 정답 단어를 읽지 않는지 확인하고 문제가 있으면 고친다.
- 커밋: `발음 듣기 추가`

## 1단계. 왼쪽 목차(사이드바)

상단 탭(퀴즈/단어장)과 일차 칩을 없애고 왼쪽 목차로 화면을 이동한다.

- 목차 항목(각각 메인 화면 하나): 오늘 `#today`, 단어장(하위에 words.js의 일차 목록 `#book-1`, `#book-2`…), 퀴즈 `#quiz`, 오답노트 `#wrong`, 기록·설정 `#settings`.
- 해시 라우팅: 새로고침·뒤로 가기에서도 같은 화면 유지. 알 수 없는 해시는 `#today`.
- 기존 퀴즈·단어장 화면은 그대로 옮긴다. 오늘·오답노트·기록·설정은 이번 단계에선 "준비 중" 자리표시 화면.
- 반응형: 너비 900px 이상이면 목차 항상 표시(약 240px). 미만이면 숨기고 좌상단 ☰ 버튼으로 드로어 열기. 바깥 누르기·항목 선택·Esc로 닫힘. 열려 있을 때 뒤 화면 스크롤 잠금.
- 현재 화면 항목 강조(`aria-current="page"`).
- 확인: 창을 좁히면 ☰로 바뀜 / `#book-1`에서 새로고침해도 유지 / 기존 퀴즈·발음 정상.
- 커밋: `왼쪽 목차 추가`

## 2단계. 일차별 기록

- 일차 퀴즈를 끝낼 때마다 그 일차의 `{ best, last, count, lastDate }` 저장. 전체 랜덤·오답 복습·주간 복습은 일차 기록에 넣지 않는다.
- 단어장에서 일차를 열면 그 일차를 `seen: true`로 기록.
- 목차의 각 일차 옆에 최고 점수를 작게 표시(`18/20`). 기록 없으면 표시 없음, 18점 이상이면 완료 표시.
- 확인: 1일차 퀴즈 후 목차에 점수 표시 / 기존 오답노트 유지.
- 커밋: `일차별 기록 저장`

## 3단계. "오늘" 화면

앱을 열면 기본으로 `#today`.

- 오늘 일차 계산: 2026-09-22 = 1일차. 월~토만 하루씩 증가, 일요일은 건너뜀(한국 시간 기준 날짜). 계산된 일차가 words.js에 없으면 words.js의 가장 큰 일차를 쓴다.
- 일요일: "총정리 날"로 표시하고, 그 주(월~토) 일차들의 단어로 20문제 복습 퀴즈를 제안.
- 구성: `오늘 · N일차`, 단어 수, 두 단계 진행 표시(① 단어장 보기 완료/미완, ② 퀴즈 최고 점수). 지금 할 차례인 것 하나만 큰 주 버튼(단어장 미확인 → "단어장 보기", 확인함 → "퀴즈 풀기", 18점 이상 → "다시 풀기"는 보조 버튼).
- 하단 보조 정보: 오답노트 개수 + 오답 복습 링크, 최근 3일 점수.
- 확인: 앱 첫 화면이 오늘 / words.js에 1일차만 있으면 1일차 표시 / 단어장 확인 후 주 버튼이 "퀴즈 풀기"로 바뀜.
- 커밋: `오늘 화면 추가`

## 4단계. 폰 한 손 조작

- 퀴즈 중 "다음 문제"/"결과 보기" 버튼을 화면 하단에 고정(`env(safe-area-inset-bottom)` 포함), 높이 최소 48px. 본문이 버튼에 가리지 않게 하단 여백 확보.
- 보기 버튼 높이 최소 48px, 간격 넉넉하게.
- 정답이면 1초 뒤 자동으로 다음 문제, 오답이면 해설에서 멈춤. 설정값(기본 켜짐)으로 끌 수 있게 하고 `prefers-reduced-motion`에서도 동작은 유지하되 애니메이션은 뺀다.
- 폰 너비에서 퀴즈 중에는 상단 제목 영역을 줄이고 진행 막대·문제 번호만 남긴다.
- 확인: 폰에서 20문제를 한 손으로 끝까지 / 자동 넘김 켜기·끄기.
- 커밋: `폰 한 손 조작 개선`

## 5단계. 퀴즈 이어하기

- 진행 중 퀴즈 상태(문제 목록·보기 순서, 현재 번호, 점수, 틀린 단어, 퀴즈 종류·범위)를 매 답안마다 localStorage에 저장.
- 퀴즈 화면과 오늘 화면에 "이어서 풀기 (7/20)" 버튼.
- 진행 중인데 새 퀴즈를 시작하면 화면 안에서 "진행 중인 퀴즈를 버리고 새로 시작할까요?" 확인.
- 퀴즈 종료 시 저장분 삭제. words.js가 바뀌어 저장된 단어를 못 찾으면 저장분을 조용히 버린다.
- 확인: 7번 문제에서 새로고침 → 이어서 풀기 / 점수·오답 정상 누적.
- 커밋: `퀴즈 이어하기`

## 6단계. 오답노트 화면

- `#wrong`: 틀린 단어 목록(단어, 뜻, 틀린 횟수, 발음 버튼), 틀린 횟수 내림차순.
- 졸업 규칙 변경: 오답 복습에서 **2번 연속** 맞혀야 오답노트에서 삭제. 틀리면 연속 횟수 0. 목록에 "졸업까지 1번" 표시. 기존 오답 데이터는 연속 0으로 옮긴다.
- 상단 "오답 복습" 버튼: 최대 20문제(오답이 적으면 있는 만큼). 뜻 고르기 위주, 예문이 있으면 빈칸 문제도 섞는다.
- 확인: 정렬 / 1번 맞히면 "졸업까지 1번", 2번 연속이면 삭제.
- 커밋: `오답노트 화면, 2연속 졸업`

## 7단계. 기록·설정 정리

- `#settings`: 일차별 점수표(일차, 최고, 마지막, 횟수, 마지막 날짜), 자동 발음·정답 시 자동 넘김 토글.
- "기록 초기화"는 이 화면 맨 아래로 이동. 누르면 화면 안에서 "정말 지울까요?" 확인 후 실행. 다른 화면에서는 제거.
- 퀴즈 결과 화면의 틀린 단어 목록에 발음 버튼 추가.
- 확인: 초기화는 두 번 눌러야 실행 / 설정 새로고침 후 유지.
- 커밋: `기록·설정 화면 정리`
